{
  "version": 2,
  "builds": [
    {
      "src": "./index.js",
      "use": "@vercel/node"
    }
  ],

  "routes": [
    { "src": "/api/auth/(.*)", "dest": "/rutes/auth.js" },
    { "src": "/api/events/(.*)", "dest": "/rutes/events.js" }
  ]
}
